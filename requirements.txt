plotly
microtc
# microtc importa numpy pero no lo declara, así que hay que pedirlo
# aquí: hoy llega por scipy, y dejaría de llegar si scipy se quita.
numpy
scipy
wordcloud
jupyter
pandas
google-colab-cli
jupyter-kernel-client<1.0.0
tqdm
# Clean filter that strips notebook outputs on commit (see CLAUDE.md). Needed
# in the container because that is where the website notebooks get edited;
# the filter itself still has to be installed per clone into .git/config.
nbstripout
